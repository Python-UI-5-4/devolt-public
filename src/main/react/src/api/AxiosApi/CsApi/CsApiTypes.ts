export type ReportPost = {
  boardId: number;
  title: string;
  content: string;
  report: string[];
};

export type SuggestionPost = {
  title: string;
  content: string;
  suggestion: string[];
};
